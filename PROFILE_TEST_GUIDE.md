# 个人中心功能测试指南

## 快速测试步骤

### 1. 启动服务器
```bash
# 确保已安装所有依赖
pip install -r requirements.txt

# 启动服务
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. 测试流程

#### 步骤1：登录系统
1. 打开浏览器访问：http://localhost:8000
2. 点击右上角"登录"
3. 使用测试账号：
   - 用户名: `georgia`
   - 密码: `georgia123`

#### 步骤2：查看个人中心
1. 登录成功后，点击右上角"个人中心"图标（👤）
2. 应该能看到以下真实数据：
   - **今日查询次数**: 12
   - **本月查询次数**: 245
   - **总查询次数**: 245
   - **常用地点**: 
     - 天津市区 (85次)
     - 北京市区 (52次)
     - 上海市区 (38次)
   - **API额度**: 245/10000
   - **存储空间**: 2.3MB/100MB
   - **最后登录时间**: 实时显示

#### 步骤3：测试实时统计
1. 返回主页（点击"地图主页"）
2. 在地图上点击不同的城市标记
3. 或使用搜索框搜索城市（如：广州、深圳等）
4. 每次查询都会自动记录
5. 再次打开个人中心，查看：
   - 今日查询次数应该增加
   - 常用地点列表可能更新
   - API额度已用数量增加

#### 步骤4：测试编辑功能
1. 在个人中心点击"✏️ 编辑信息"
2. 填写邮箱和姓名
3. 点击"保存修改"
4. 页面刷新后应显示更新的信息

#### 步骤5：测试修改密码
1. 点击"🔒 修改密码"
2. 输入当前密码：`georgia123`
3. 输入新密码（至少6位）
4. 确认新密码
5. 提交后应提示"密码修改成功"

## 预期结果

### ✅ 个人中心应显示
- [x] 实时的今日查询次数
- [x] 实时的本月查询次数
- [x] 实时的总查询次数
- [x] 常用地点列表（最多5个，按次数排序）
- [x] API使用额度
- [x] 存储空间使用情况
- [x] 用户邮箱和姓名
- [x] 最后登录时间
- [x] 注册时间

### ✅ 统计功能应工作
- [x] 每次在主页查询天气时自动记录
- [x] 查询次数实时累加
- [x] 常用地点自动统计和排序
- [x] 跨天后今日查询次数重置为0

## 调试技巧

### 查看后端日志
服务器终端会显示所有API调用：
```
INFO:     127.0.0.1:xxxxx - "GET /api/user-stats HTTP/1.1" 200 OK
INFO:     127.0.0.1:xxxxx - "POST /api/record-query HTTP/1.1" 200 OK
```

### 查看用户数据
直接查看 `users.json` 文件内容：
```bash
cat users.json
# 或在Windows上
type users.json
```

### 浏览器控制台
1. 按F12打开开发者工具
2. 切换到Console标签
3. 查看是否有错误信息
4. 可以手动测试API：
```javascript
// 获取统计数据
fetch('/api/user-stats').then(r => r.json()).then(console.log)

// 记录查询
fetch('/api/record-query', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({location: '测试城市'})
}).then(r => r.json()).then(console.log)
```

## 常见问题

### Q1: 个人中心显示"加载中..."不消失
**解决方案**：
1. 检查是否已登录
2. 打开浏览器控制台查看错误
3. 确认服务器正常运行
4. 检查用户数据文件users.json是否存在

### Q2: 查询不计数
**解决方案**：
1. 确认已登录
2. 查看浏览器控制台是否有错误
3. 检查后端日志是否有/api/record-query调用
4. 查看users.json是否更新

### Q3: 数据不更新
**解决方案**：
1. 刷新个人中心页面（F5）
2. 清除浏览器缓存
3. 检查users.json文件权限
4. 重启服务器

### Q4: 老用户看不到统计数据
**解决方案**：
- 首次访问个人中心时会自动初始化stats字段
- 刷新页面后应该能看到初始数据（全部为0）

## 新用户注册测试

### 测试新注册用户
1. 登出当前账号
2. 点击"注册"
3. 填写信息：
   - 用户名: `testuser`
   - 密码: `test123456`
   - 邮箱: `test@example.com`
4. 注册成功后登录
5. 查看个人中心，应显示初始统计数据（全为0）
6. 进行几次查询后，统计数据应更新

## API端点测试

使用curl或Postman测试API：

```bash
# 1. 获取用户统计（需要先登录获取cookie）
curl -X GET http://localhost:8000/api/user-stats \
  -H "Cookie: access_token=YOUR_TOKEN"

# 2. 记录查询
curl -X POST http://localhost:8000/api/record-query \
  -H "Content-Type: application/json" \
  -H "Cookie: access_token=YOUR_TOKEN" \
  -d '{"location":"测试城市"}'

# 3. 更新个人信息
curl -X POST http://localhost:8000/api/update-profile \
  -H "Content-Type: application/json" \
  -H "Cookie: access_token=YOUR_TOKEN" \
  -d '{"email":"new@example.com","full_name":"新名字"}'
```

## 验收标准

### 基本功能 ✅
- [ ] 个人中心页面正常加载
- [ ] 显示真实的用户统计数据
- [ ] 查询天气时自动记录统计
- [ ] 编辑个人信息功能正常
- [ ] 修改密码功能正常

### 数据准确性 ✅
- [ ] 查询次数准确累计
- [ ] 常用地点正确排序
- [ ] API额度正确显示
- [ ] 时间信息准确

### 用户体验 ✅
- [ ] 页面加载流畅
- [ ] 数据更新及时
- [ ] 操作反馈明确
- [ ] 错误提示友好

## 成功标志

当你能完成以下操作时，说明功能完全正常：

1. ✅ 登录后能看到个人中心的真实统计数据
2. ✅ 在主页查询天气后，个人中心的统计会更新
3. ✅ 常用地点列表会根据查询自动更新
4. ✅ 能成功编辑个人信息
5. ✅ 能成功修改密码
6. ✅ 新注册用户也有完整的统计功能

祝测试顺利！🎉
