# 智能出行规划 Agent 使用指南

## 功能概述

智能出行规划 Agent（Travel Planner Agent）是一个基于 AI 的出行规划助手，可以根据实时天气数据和用户的出行需求，为用户推荐最优的出行方案。

## 核心特性

### 1. **多模式出行规划**
支持以下出行方式的评估和推荐：
- 🚇 **地铁** - 准时可靠，不受天气影响
- 🚌 **公交** - 经济实惠，轻微天气影响
- 🚗 **自驾** - 灵活方便，易受天气影响
- 🚶 **步行** - 健康环保，易受恶劣天气影响
- 🚲 **骑车** - 低碳出行，受降雨和强风影响大

### 2. **天气影响分析**
基于以下天气因素进行智能评估：
- 🌡️ **温度** - 极端温度（<-10°C 或 >35°C）影响户外出行
- 🌧️ **降雨概率** - 高降雨概率限制户外出行方式
- 💨 **风速** - 强风影响自驾和开放式交通
- 💧 **湿度** - 辅助出行舒适度评估

### 3. **智能路线规划**
提供多条推荐路线，包含：
- 📍 **路线名称** - 清晰的路线描述
- ⏱️ **预计时长** - 基于当前路况的出行时间
- 📏 **距离** - 路线总距离
- 🚦 **路况等级** - 实时路况评估
- 📋 **详细说明** - 关键提示和转换点

### 4. **出行时间优化**
根据天气和路况推荐：
- ⏰ **最优出发时间** - 规避拥堵和不良天气时段
- ⌛ **出行时长预估** - 准确的时间规划

### 5. **路况预测**
提供实时和预测的路况信息：
- 🚦 **当前路况** - smooth（畅通）/ moderate（适中）/ congested（拥堵）/ severe（严重拥堵）
- ⏲️ **拥堵高峰** - 预测拥堵时段
- 💡 **路况建议** - 根据预测给出建议

### 6. **出行建议清单**
基于综合分析提供：
- 携带物品建议
- 穿着建议
- 时间规划建议
- 安全注意事项

## 访问方式

### 网页界面
访问地址：`/travel-planning`
- 需要先登录账户
- 提供友好的表单界面
- 实时展示规划结果

### API 接口
```
POST /api/travel-planning?lat=<纬度>&lng=<经度>&city=<城市名>
Content-Type: application/json

请求体：
{
    "origin": "天津和平广场",
    "destination": "天津滨河公园",
    "preferred_time": "09:00",  // 可选
    "preferred_modes": ["subway", "bus"],  // 可选
    "expected_duration": 30,  // 可选，分钟
    "distance": 10  // 可选，公里
}

响应示例：
{
    "primary_mode": "subway",
    "modes_rating": [...],
    "recommended_routes": [...],
    "departure_time": "09:00",
    "travel_duration": 25,
    "traffic_prediction": {...},
    "weather_impact": {...},
    "suggestions": [...],
    "confidence_score": 0.85
}
```

## 使用步骤

### 1. 在网页上使用

**第一步：填写出行信息**
```
- 出发地点（必填）：例如 "天津和平广场"
- 目的地（必填）：例如 "天津滨河公园"
- 距离（可选）：例如 "10 公里"
- 预期时长（可选）：例如 "30 分钟"
```

**第二步：设置时间信息**
```
- 偏好出发时间（可选）：使用时间选择器
- 所在城市/地点（必填）：下拉选择列表
```

**第三步：选择出行方式偏好**
```
- 点击相应的按钮选中偏好的出行方式
- 可以选择多种方式
- 留空则考虑所有方式
```

**第四步：点击"规划出行方案"**
- 系统将获取实时天气数据
- 使用 AI 模型进行智能规划
- 返回详细的出行建议

### 2. 通过 API 调用

```python
import httpx
import json
from datetime import datetime

# 调用 API
response = httpx.post(
    "http://localhost:8000/api/travel-planning?lat=39.0851&lng=117.1994&city=天津",
    json={
        "origin": "天津和平广场",
        "destination": "天津滨河公园",
        "preferred_time": "09:00",
        "preferred_modes": ["subway", "bus"],
        "expected_duration": 30,
        "distance": 10
    }
)

result = response.json()
print(json.dumps(result, indent=2, ensure_ascii=False))
```

## 出行方式选择标准

### 🚇 地铁
- **GO（推荐）**：任何天气条件下
- **CAUTION（谨慎）**：极端恶劣天气可能影响运营
- **AVOID（避免）**：几乎不会被天气影响

### 🚌 公交
- **GO（推荐）**：正常天气
- **CAUTION（谨慎）**：大雨或强风时
- **AVOID（避免）**：极端天气（暴雨、台风等）

### 🚗 自驾
- **GO（推荐）**：天气良好，能见度好
- **CAUTION（谨慎）**：降雨概率 30-70%，或风速 5-8 m/s
- **AVOID（避免）**：
  - 降雨概率 > 70% 且风速 > 6 m/s
  - 风速 > 10 m/s
  - 能见度极差的恶劣天气

### 🚶 步行
- **GO（推荐）**：晴朗温暖的天气
- **CAUTION（谨慎）**：
  - 降雨概率 30-50%
  - 温度在 5-35°C 之间
- **AVOID（避免）**：
  - 降雨概率 > 70%
  - 温度 < 0°C 或 > 35°C
  - 风速 > 6 m/s

### 🚲 骑车
- **GO（推荐）**：
  - 降雨概率 < 30%
  - 风速 < 5 m/s
  - 温度适宜
- **CAUTION（谨慎）**：
  - 降雨概率 30-50%
  - 轻微风（5-8 m/s）
- **AVOID（避免）**：
  - 降雨概率 > 50%
  - 风速 > 8 m/s
  - 极端温度

## 天气影响等级说明

### 🟢 Minimal（轻微）
- 天气条件良好
- 对出行影响很小
- 可正常使用各种出行方式

### 🟡 Moderate（中等）
- 天气条件一般
- 对某些出行方式有影响
- 建议遵循推荐方式

### 🟠 Significant（显著）
- 天气条件较恶劣
- 多数户外出行方式受限
- 强烈建议使用地铁/公交

### 🔴 Severe（严重）
- 恶劣天气条件
- 多数出行方式不可行
- 建议延迟出行或选择备选路线

## 实例分析

### 场景 1：晴天上班

**输入：**
- 出发地：天津和平广场
- 目的地：天津滨河公园
- 城市：天津
- 天气：晴朗，温度 20°C，降雨概率 5%，风速 2 m/s

**输出：**
- 🚇 地铁：GO（推荐）
- 🚌 公交：GO（推荐）
- 🚗 自驾：GO（推荐）
- 🚶 步行：GO（推荐）
- 🚲 骑车：GO（推荐）
- **首选：地铁**（最快、最稳定）
- 建议出发时间：08:45（避开高峰）

### 场景 2：下雨天出行

**输入：**
- 出发地：北京市区中心
- 目的地：北京首都机场
- 城市：北京
- 天气：中雨，温度 15°C，降雨概率 75%，风速 4 m/s

**输出：**
- 🚇 地铁：GO（推荐）✓
- 🚌 公交：CAUTION（谨慎）
- 🚗 自驾：CAUTION（谨慎）
- 🚶 步行：AVOID（避免）
- 🚲 骑车：AVOID（避免）
- **首选：地铁**（完全不受雨水影响）
- 建议：携带雨具，穿着防水衣物
- 出行建议：考虑预留 10-15 分钟等待地铁

### 场景 3：高温出行

**输入：**
- 出发地：深圳市区
- 目的地：深圳机场
- 城市：深圳
- 天气：晴朗高温，温度 38°C，降雨概率 10%，风速 3 m/s

**输出：**
- 🚇 地铁：GO（推荐）✓
- 🚌 公交：GO（推荐）
- 🚗 自驾：GO（推荐）
- 🚶 步行：CAUTION（谨慎）
- 🚲 骑车：CAUTION（谨慎）
- **首选：地铁或公交**（避免高温暴晒）
- 建议：
  - 出发时间：早上 07:00 之前或晚上 19:00 之后
  - 携带充足的水和防晒物品
  - 穿着轻薄透气衣物

### 场景 4：强风天气

**输入：**
- 出发地：上海市区
- 目的地：上海外滩
- 城市：上海
- 天气：多云，温度 18°C，降雨概率 20%，风速 9 m/s

**输出：**
- 🚇 地铁：GO（推荐）✓
- 🚌 公交：GO（推荐）
- 🚗 自驾：CAUTION（谨慎）
- 🚶 步行：CAUTION（谨慎）
- 🚲 骑车：AVOID（避免）
- **首选：地铁或公交**
- 建议：
  - 避免骑车出行
  - 驾驶时小心大风侧风
  - 步行时注意风向，避免高大建筑物附近

## 技术架构

### 核心组件

**1. TravelPlannerAgent**
- 位置：`app/agents/travel_planner_agent.py`
- 功能：
  - 整合天气数据和出行需求
  - 调用 LLM 进行智能分析
  - 解析和返回结构化结果

**2. 数据模型**
- 位置：`app/models/schemas.py`
- 关键类：
  - `TravelPlanningRequest` - 出行规划请求
  - `TravelPlanningResponse` - 规划响应
  - `TransportMode` - 出行方式评级
  - `Route` - 推荐路线
  - `TrafficPrediction` - 路况预测
  - `WeatherImpact` - 天气影响分析

**3. API 路由**
- 位置：`app/main.py`
- 端点：`POST /api/travel-planning`
- 页面：`GET /travel-planning`

**4. 前端界面**
- 位置：`app/templates/travel_planning.html`
- 功能：
  - 出行信息表单
  - 实时结果展示
  - 交互式出行方式选择

### 工作流程

```
用户提交请求
  ↓
获取实时天气数据 (Open-Meteo API)
  ↓
构建 Prompt 信息
  ↓
调用 LLM (OpenAI/DeepSeek/Mock)
  ↓
解析 JSON 响应
  ↓
返回结构化结果
  ↓
前端展示规划结果
```

## 环境配置

### 必需环境变量

```env
# LLM 配置
LLM_PROVIDER=deepseek  # 或 openai, mock
LLM_MODEL=deepseek-chat
DEEPSEEK_API_KEY=your_key_here
OPENAI_API_KEY=your_key_here  # 如果使用 OpenAI
```

### Python 依赖

```
fastapi>=0.104.0
httpx>=0.25.0
pydantic>=2.0.0
python-dotenv>=1.0.0
openai>=1.0.0  # 用于调用 LLM
```

## 常见问题

### Q1：为什么推荐地铁而不是自驾？
**A：** 地铁在任何天气条件下都是最稳定和准时的选择，即使在恶劣天气下也不受影响。自驾可能因为降雨、强风或拥堵而导致延误。

### Q2：API 返回置信度低怎么办？
**A：** 置信度低通常意味着：
- 天气数据不完整或实时性不强
- 出行信息不够详细
- 建议参考多个路线或方式

### Q3：为什么某些天气条件下不建议步行？
**A：** 因为：
- 降雨会让人身体不适，容易感冒
- 强风可能导致安全隐患
- 极端温度（过冷或过热）对身体有害

### Q4：能否离线使用？
**A：** 不能。该系统需要：
- 实时天气数据（Open-Meteo API）
- LLM 支持（OpenAI/DeepSeek 等）
- 所有请求需要网络连接

### Q5：路况预测的准确性如何？
**A：** 当前基于：
- 历史出行数据
- 天气影响分析
- AI 模型推理

准确性会随着更多真实数据积累而提高。

## 扩展计划

### 未来功能

1. **实时路况集成**
   - 接入高德地图/百度地图 API
   - 获取真实路况数据

2. **用户偏好学习**
   - 记录用户出行历史
   - 个性化推荐

3. **多城市支持**
   - 扩展到全国所有主要城市
   - 考虑地方特色交通

4. **成本分析**
   - 计算各种方式的成本
   - 时间 vs 成本 的权衡

5. **环保评分**
   - 计算碳排放量
   - 推荐最绿色的方案

6. **社交分享**
   - 分享出行方案
   - 实时邀请同行

## 反馈与改进

欢迎提交：
- 功能建议
- bug 报告
- 用户体验反馈

通过改进此系统，帮助更多用户做出更好的出行决策！
