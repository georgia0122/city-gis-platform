# AI 助手文件上传与分析功能使用指南

## 功能概述

现在您的 GeoWeather AI 助手支持以下强大的文件处理功能：

### 1. **文件上传与自动分析**
- 支持上传多种格式的文件和图片
- 自动检测和优化文件
- 智能文本提取和内容分析

### 2. **深度 AI 分析**
- 使用 AI 对上传文件进行专业分析
- 生成详细的见解和建议
- 支持多文件对比分析

### 3. **图片分析**
- 自动提取图片中的主要颜色
- 识别图片尺寸和格式信息
- 支持透明度和灰度检测

### 4. **文档处理**
- **PDF**：智能文本提取，支持多页面
- **Word**：提取段落、表格等结构化内容
- **文本文件**：自动编码检测和内容分析

## 支持的文件格式

### 图片格式
- PNG, JPG/JPEG, GIF, WEBP, BMP

### 文档格式
- PDF (最多读取前5页)
- Word 文档 (.docx, .doc)
- 文本文件 (.txt)

### 文件大小限制
- 单个文件最大 10MB
- 建议 5MB 以下获得最佳体验

## 使用方法

### 快速使用流程

#### 方式 1：即时发送（与聊天集成）
1. 点击下方的 **"图片"** 或 **"文件"** 按钮
2. 选择要上传的文件
3. 文件会显示在预览区
4. 输入问题提示或直接点击发送按钮
5. AI 助手会自动分析文件和回答问题

#### 方式 2：深度分析（专业分析）
1. 上传一个或多个文件
2. 点击 **"分析"** 按钮（蓝紫色按钮）
3. 系统会进行深度 AI 分析
4. 收到详细的分析结果和专业建议

### 详细操作步骤

#### 上传图片

```
1. 点击聊天框上方的 📷 "图片" 按钮
2. 选择一张或多张图片
3. 图片会显示在预览区
4. 可以：
   - 输入问题后发送（如"分析这张图片的颜色搭配"）
   - 直接点击"分析"按钮进行深度分析
```

#### 上传文档

```
1. 点击 📄 "文件" 按钮
2. 选择 PDF、Word 或文本文件
3. 系统自动提取内容
4. 您可以：
   - 提问相关内容（如"这份文档的核心概点是什么"）
   - 进行深度分析和总结
   - 与其他文件对比分析
```

#### 分析多个文件

```
1. 依次上传多个文件（最多支持同时处理10个）
2. 所有文件都会显示在预览区
3. 点击"分析"按钮
4. AI 会对所有文件进行综合分析，包括：
   - 文件之间的关联性
   - 共同的主题和内容
   - 综合的见解和建议
```

## API 端点说明

### 1. 上传并分析文件
```
POST /api/upload-file
Content-Type: multipart/form-data

参数:
- files: 文件对象数组

返回:
{
    "files": [
        {
            "filename": "图片.jpg",
            "type": "image",
            "format": "jpeg",
            "size": 102400,
            "dimensions": [1920, 1080],
            "base64": "...",
            "analysis": {...},
            "summary": "..."
        }
    ],
    "success_count": 1,
    "error_count": 0,
    "errors": []
}
```

### 2. 深度 AI 分析
```
POST /api/analyze-files
Content-Type: multipart/form-data

参数:
- files: 文件对象数组

返回:
{
    "files": [文件基本信息],
    "ai_analysis": "AI 深度分析结果",
    "analysis_method": "ai" 或 "basic",
    "errors": []
}
```

### 3. 聊天接口（支持文件上传）
```
POST /api/chat
Content-Type: multipart/form-data

参数:
- message: 用户消息
- history: 对话历史（JSON）
- files: 上传的文件数组

返回:
{
    "reply": "AI 回复",
    "files_processed": 1,
    "images_processed": 0
}
```

## 文件分析的工作原理

### 图片分析流程
```
上传 → 压缩优化 → 主色提取 → 格式检测 → Base64编码 → 显示预览
```

### 文档分析流程
```
上传 → 编码检测 → 内容提取 → 统计分析 → 生成摘要 → AI 分析
```

### AI 分析流程
```
收集所有文件摘要 → 构建分析提示词 → 调用 LLM → 整合结果 → 返回分析
```

## 示例使用场景

### 场景 1：快速查看天气图表
```
用户: 上传天气图表 → 输入"这个图表显示了什么" → 发送
AI: 自动分析图表内容，提供解释
```

### 场景 2：分析多个文档
```
用户: 上传3个出行计划文件 → 点击"分析"按钮
AI: 综合分析所有文件，识别冲突和重复部分，提供优化建议
```

### 场景 3：内容总结
```
用户: 上传 PDF 文档 → 输入"给我总结核心要点"
AI: 提取 PDF 内容，进行智能总结和提炼
```

### 场景 4：数据提取
```
用户: 上传包含数据的 Word 文档 → 输入"提取所有数字数据"
AI: 识别和提取文档中的所有数值信息
```

## 常见问题

### Q: 上传的文件安全吗？
A: 文件仅在分析期间在服务器内存中处理，分析完成后立即删除。不会长期存储用户文件。

### Q: 支持的文件大小有限制吗？
A: 单文件限制 10MB，建议使用 5MB 以下的文件以获得最佳性能。

### Q: 网络不好时上传会卡吗？
A: 前端会检查文件有效性，网络超时会有相应提示。大文件建议在网络稳定时上传。

### Q: 可以同时上传多少个文件？
A: 理论上无限制，但建议不超过 10 个以获得最佳体验。

### Q: 分析需要多长时间？
A: 取决于文件数量和大小：
- 单个图片：秒级
- 单个 PDF（5页）：1-2 秒
- 多个文件深度分析：2-5 秒

### Q: AI 分析失败怎么办？
A: 系统会自动降级到基础分析，返回文件的统计信息和摘要。

## 技术实现细节

### 后端技术栈
- **图片处理**：Pillow (PIL)
- **PDF 处理**：PyPDF2
- **Word 处理**：python-docx
- **文本编码**：自动检测 (UTF-8, GBK, GB2312 等)
- **AI 分析**：OpenAI API / DeepSeek API

### 前端技术栈
- **文件处理**：原生 JavaScript File API
- **异步上传**：Fetch API + FormData
- **预览渲染**：Canvas 和 DOM 操作

## 性能优化建议

1. **图片优化**
   - 上传前压缩图片到 5MB 以下
   - 使用现代格式（WebP）获得更小的文件体积

2. **批量上传**
   - 一次性上传所有相关文件
   - 避免多次单个上传

3. **网络优化**
   - 在有线网络环境进行大文件上传
   - 避免在弱信号区域操作

## 未来功能计划

- [ ] OCR 文字识别（从图片提取文字）
- [ ] 表格识别和数据提取
- [ ] Excel 文件支持
- [ ] 视频截图分析
- [ ] 文件版本管理
- [ ] 分析历史记录
- [ ] 自定义分析规则

## 反馈与支持

如遇到任何问题，请：
1. 检查文件格式和大小
2. 查看浏览器控制台错误信息
3. 尝试刷新页面重新上传
4. 联系技术支持

---

**最后更新**：2026年2月15日
**版本**：1.0
